# Development and testing dependencies
# Install with: pip install -r requirements-dev.txt

# Testing
pytest>=8.0
pytest-cov>=5.0

# The Azure SDK is NOT required for running the test suite.
# All Azure dependencies are mocked in tests/conftest.py.
# To run the functions against live Azure, install functions/requirements.txt.
